---
__extend: skeleton

package:
  includes:
    - "*"

deploy:
  owner: apache
  group: apache
  mode: 755

  instance:
    post:
      - touch [% root.deploy.locations.0 %]/[% root.vca.appName %]/config/wsgi.py
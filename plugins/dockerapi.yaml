---
__extend: skeleton

build:
  builder: maven
  cmds: 
    - "(cd %AppRoot%; mvn clean package;)"
  pre: 
    - "$CIOM_SCRIPT_HOME/pre.build.sh %Version% %CloudId% %AppName%"
  post: []

package:
  includes:
    - "*"

deploy:
  mode: '755'
